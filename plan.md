# md-viewer 実装計画

## 目的

Go標準ライブラリだけを使い、Markdown文書をANSI装飾付きでコンソール表示できるCLIを作る。

## 対象機能

- ファイルまたは標準入力からMarkdownを読み込む
- 見出し、段落、リスト、引用、区切り線、コードブロックを表示する
- 太字、斜体、取り消し線、インラインコード、リンクを装飾する
- GFM風パイプテーブルを罫線付きで表示する
- 日本語などの全角文字を考慮して折り返しと表の列幅を計算する
- ANSIカラーを使用し、`-no-color` と `NO_COLOR` で無効化できるようにする
- 対話端末ではページ単位で閲覧できるようにする
- 外部ライブラリには依存しない

## CLI仕様

```text
md-viewer [options] [file]
cat README.md | md-viewer
```

オプション:

- `-no-color`: ANSIカラーを無効化
- `-ascii`: テーブルと区切り線をASCII文字で描画
- `-no-pager`: ページャーを無効化
- `-width N`: 表示幅を指定
- `-watch`: ファイル変更時に自動で再描画

表示幅と高さは、それぞれ `COLUMNS` と `LINES` を参照する。未設定時は幅80、高さ24を既定値とする。

## 表示方針

- レベル1見出し: 明るいシアン、太字、罫線
- レベル2見出し: 明るい青、太字
- レベル3以下: 明るい白、太字
- コード: 黄色
- 引用: グレー
- リンク: 青、下線
- 背景色と大型文字は使用しない

## テーブル

- ヘッダー区切り行からテーブルを判定する
- `:---`、`:---:`、`---:` の配置指定に対応する
- `\|` をセル内のパイプとして扱う
- 端末幅を超える場合は広い列から縮め、セル内を折り返す
- Unicode罫線を標準とし、`-ascii` では `+`、`-`、`|` を使う

## ページャー

対話端末では、レンダリング結果をシステムの `less -R` に渡す。これにより、ANSIカラーを維持しながら、スクロール、検索、先頭・末尾移動など、普段の `less` と同じ操作を利用できる。

代表的な操作:

- `j` / `k`、矢印キー: 1行移動
- Space / `b`: 1画面移動
- `g` / `G`: 文書の先頭／末尾へ移動
- `/文字列`: 前方検索
- `n` / `N`: 次／前の検索結果
- `q`: 終了

`less` がインストールされていない場合は、既存の行入力方式ページャーへフォールバックする。標準入力から文書を読んでいてフォールバック用の操作入力を確保できない場合は、全体を通常出力する。Goモジュールとしての外部ライブラリ依存は追加しない。

## 構成

```text
main.go                    CLI、入力、オプション処理
internal/markdown/         Markdownのブロック解析と文書ノード
internal/render/           ANSI装飾、折り返し、テーブル描画
internal/pager/            行入力方式のページャー
```

## 実装順序

1. CLIと入力処理
2. Markdownブロック解析
3. インライン装飾とANSIレンダリング
4. Unicode表示幅と折り返し
5. テーブル解析と描画
6. ページャー
7. 単体テスト、`go vet`、動作確認

## 完了条件

- `go test ./...` が成功する
- `go vet ./...` が成功する
- `go.mod` に外部モジュールが含まれない
- ファイル入力とパイプ入力が動作する
- 日本語を含む見出し、本文、テーブルを表示できる
- カラー、カラー無効化、ASCII罫線、ページャーが動作する
